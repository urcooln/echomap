# EchoMap uses artifact-specific env files in local development.
#
# Copy these files when setting up a new machine:
#   cp artifacts/api-server/.env.example artifacts/api-server/.env
#   cp artifacts/echomap/.env.example artifacts/echomap/.env
#
# Do not commit real .env files. They can contain Clerk, database, AI, and
# encryption secrets.
